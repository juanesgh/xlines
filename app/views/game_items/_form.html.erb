<%= form_with(model: game_item, local: true) do |form| %>
  <% if game_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_item.errors.count, "error") %> prohibited this game_item from being saved:</h2>

      <ul>
        <% game_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.text_field :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
